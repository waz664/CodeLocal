#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "$0")" && pwd)"
export PYTHONPATH="$repo_root/codelocal${PYTHONPATH:+:$PYTHONPATH}"
cd "$repo_root"
exec python3 -m codelocal_agent "$@"
